<template>
  <nav class="bg-indigo-600 shadow-lg">
    <div class="max-w-7xl mx-auto px-4">
      <div class="flex items-center justify-between h-16">
        <div class="flex items-center">
          <router-link to="/" class="text-white font-bold text-xl flex items-center">
            <span class="mr-2">📱</span>
            Meta SP Manager
          </router-link>
        </div>
        <div class="hidden md:block">
          <div class="ml-10 flex items-baseline space-x-4">
            <router-link
              v-for="route in routes"
              :key="route.name"
              :to="{ name: route.name }"
              class="text-white hover:bg-indigo-700 px-3 py-2 rounded-md text-sm font-medium transition-colors"
              active-class="bg-indigo-800"
            >
              {{ route.label }}
            </router-link>
          </div>
        </div>
        <div class="md:hidden">
          <button
            @click="isMobileMenuOpen = !isMobileMenuOpen"
            class="text-white hover:bg-indigo-700 p-2 rounded-md"
          >
            <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path v-if="!isMobileMenuOpen" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
              <path v-else stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>
      </div>
    </div>
    <div v-if="isMobileMenuOpen" class="md:hidden bg-indigo-700">
      <div class="px-2 pt-2 pb-3 space-y-1 sm:px-3">
        <router-link
          v-for="route in routes"
          :key="route.name"
          :to="{ name: route.name }"
          @click="isMobileMenuOpen = false"
          class="text-white hover:bg-indigo-800 block px-3 py-2 rounded-md text-base font-medium"
          active-class="bg-indigo-800"
        >
          {{ route.label }}
        </router-link>
      </div>
    </div>
  </nav>
</template>

<script setup lang="ts">
import { ref } from 'vue'

const isMobileMenuOpen = ref(false)

const routes = [
  { name: 'Home', label: 'Home' },
  { name: 'Dashboard', label: 'Dashboard' },
  { name: 'WabaManagement', label: 'WABA Management' },
  { name: 'Templates', label: 'Templates' },
  { name: 'Analytics', label: 'Analytics' },
  { name: 'Settings', label: 'Settings' },
  { name: 'PrivacyPolicy', label: 'Privacy Policy' },
  { name: 'TermsOfService', label: 'Terms of Service' },
  { name: 'DataDeletion', label: 'Data Deletion' }
]
</script>
